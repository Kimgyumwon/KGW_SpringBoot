<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
	<mapper namespace="com.kgw.app.board.qna.QnaDAO">
	
		<select id="list" resultType="QnaDTO" parameterType="Pager">
			SELECT * FROM qna ORDER BY board_num DESC LIMIT #{startNum} , #{perPage}
		</select>
		
		<insert id="add" parameterType="QnaDTO">
			INSERT INTO qna
			(board_title, board_contents, board_writer, board_ref, board_step, board_depth)
			VALUES
			(#{boardTitle}, #{boardContents},#{boardWriter},  #{boardRef}, #{boardStep}, #{boardDepth})
		</insert>
		
		<select id="count" resultType="Long">
			SELECT COUNT(*) FROM qna
		</select>
		
		
	</mapper>